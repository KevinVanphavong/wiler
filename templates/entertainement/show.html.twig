{% extends 'layout.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
    <section class="container pb-5">
        <h1 class="text-center my-5">Evènement "{{ entertainement.name }}"</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entertainement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entertainement.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entertainement.description }}</td>
            </tr>
            </tbody>
        </table>

        <div class="d-inline-flex">
            <a href="{{ path('admin_entertainement_index') }}" class="btn btn-primary mr-2">Retour</a>

            <a href="{{ path('admin_entertainement_edit', {'id': entertainement.id}) }}" class="btn btn-warning mr-2">Modifier</a>

            {{ include('entertainement/_delete_form.html.twig') }}
        </div>


    </section>


{% endblock %}
